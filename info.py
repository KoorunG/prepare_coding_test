# 리트코드 기본 라이브러리
import collections          # deque, Counter, OrderDict 등 특수 컨테이너 데이터형을 제공하는 라이브러리
import heapq                # 힙(heap)과 큐(queue) 관련 라이브러리
import functools            # 고차함수 관련 문제해결에 유용한 라이브러리
import itertools            # 순열, 조합 관련 문제해결에 유용한 라이브러리
import re                   # 정규식(regular expression) 라이브러리
import sys                  # 인터프리터가 제공하는 변수 및 함수를 직접제어 하기 위한 라이브러리
import math                 # math
import bisect               # 이분탐색 라이브러리
from typing import *        # 파이썬 타입지정 라이브러리


# deque = collections.deque()
# defaultdict = collections.defaultdict()
# print(type(deque))
# print(type(defaultdict))